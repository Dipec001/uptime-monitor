groups:
  - name: test-alerts
    rules:
      - alert: AlwaysFires
        expr: vector(1)  # Always true
        for: 10s
        labels:
          severity: critical
        annotations:
          summary: "Test alert from Divine"
          description: "This is a test alert that always fires to verify Email integration."